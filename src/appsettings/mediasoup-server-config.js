// server-config.js
module.exports = {
    listenIp: '0.0.0.0',  // O endereço IP no qual o servidor deve escutar. '0.0.0.0' significa que ele vai escutar em todas as interfaces de rede.
    listenPort: 3000,    // A porta em que o servidor deve escutar.
    // sslCrt: 'cert.pem',  // O caminho para o certificado SSL (usado se você deseja habilitar HTTPS).
    // sslKey: 'key.pem',   // O caminho para a chave privada SSL (usado se você deseja habilitar HTTPS).
};
  